# 部署说明（WorkBuddy 工作台）

本压缩包是一个**单文件 HTML 工作台**，无需构建、无后端、无依赖，解压即用。

## 包含文件

| 文件 | 作用 |
|---|---|
| `index.html` | 工作台主程序（已含电车管理「多周期经验标定」改版） |
| `README.md` | 本次电车管理改动与使用说明 |
| `netlify.toml` | Netlify 部署配置（可选，放根目录即可） |
| `DEPLOY.md` | 本文件 |

## 方式一：Netlify 拖拽部署（最快）

1. 解压本压缩包。
2. 打开 https://app.netlify.com/drop ，把解压后的**整个文件夹**拖进去。
3. 几秒后获得一个 `xxx.netlify.app` 地址，直接访问即可。
4. 以后更新：把新的 `index.html` 拖进去替换即可，旧链接不变。

> 若下载仓库方式部署，把 `netlify.toml` 一起上传，可保证路径兜底与「不缓存最新版」。

## 方式二：任意静态托管（GitHub Pages / Vercel / 对象存储 / 自己的服务器）

- 只要把 `index.html` 放到网站根目录（或对应站点目录）即可。
- 无需任何构建步骤、无需环境变量。
- 域名绑定、HTTPS 按各平台常规方式配置。

## 方式三：本地直接打开

- 双击 `index.html` 用浏览器打开即可使用（数据存在浏览器 localStorage，不联网也可用）。

## 数据说明

- 所有记录（含电车电池健康记录）保存在**浏览器本地存储**，不上传服务器。
- 换设备/清缓存会丢失，建议定期导出备份（工作台内有导出功能）。

## 本次更新要点（电车管理）

- **实际续航**：改为「免充电效率」算法 `续航 = Σ里程 ÷ Σ(电量下降%)`，只用里程 + SoC 起止，彻底绕开不可靠的车机 kWh。
- **容量 / 健康度**：充电标定优先 `容量 = Σ(墙表度数×效率) ÷ Σ(电量%)`，效率按气温自动估、多周期汇总摊平波动。
- **录入新增**：每次充电周期末次行程可补填「墙表度数 + 气温」用于标定。
- 详见 `README.md`。
