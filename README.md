# 个人工作台

用于管理工作与生活记录的单页工作台，支持电脑和手机浏览器使用。页面代码、样式及交互集中在 [`index.html`](./index.html) 中，无需构建即可打开。

## 快速使用

本地验证：用 Edge 或 Chrome 打开 `index.html`。修改文件后刷新页面即可查看效果。

GitHub 发布：将 `index.html` 和本文件放在仓库的同一目录，将该目录用作 GitHub Pages 的发布来源。README 会显示在仓库页面，工作台由 `index.html` 提供。

已有站点：[打开个人工作台](https://j2pm8zdjr9-maker.github.io/xjjgzhang/)。本说明不表示最新本地修改已经发布到该站点。

## 主要功能

| 分类 | 功能 |
| --- | --- |
| 工作 | 待办任务、工作留痕、日程、纪检监察相关记录 |
| 学习与文化 | 备考、影剧书记录、阅读及观看日志 |
| 健康与生活 | 身体管理、饮水、打卡、日记、宝宝成长 |
| 家庭与资产 | 财务、车辆、物品、家人和宠物档案 |
| 汇总与设置 | 首页总览、时光档案、主题与字号、数据备份 |

### 影剧书记录

- 封面墙每行固定三个封面；标题最多一行，超长省略，下方显示评分和状态；已看完且填写结束日期时显示“2026.9.17看完”。完整标题、分类和日期请点击封面查看详情。
- 点击封面进入详情，可编辑、删除，以及将未结束的作品标记为“看完”或“弃了”。
- 电影：记录观看日期和时长。
- 电视剧：记录观看日期、集数和时长。集数可填写“第3集”“第3—5集”或“第2季第1集”。
- 书籍：记录阅读日期、起止页码和时长。
- 历史阅读/观看日志继续保留；旧电视剧日志缺少集数时显示“未记录集数”。

### 时光档案

- 同一天、通过来源任务编号明确关联的任务与工作留痕合并展示，原始记录保留。
- 同名但没有关联、或不同日期的记录不自动合并。
- 任务仅展示已完成项，隐藏待办和进行中任务及其关联留痕。
- 未来日期的工作留痕、未完成的学习计划不展示。
- 身体测量使用中文名称及单位，例如“体重 80.45 公斤”。

## 数据与备份

业务数据保存在当前设备的浏览器本地存储中。GitHub 存放页面代码，不会自动同步电脑和手机的个人记录。本地文件页面与 GitHub Pages 页面也使用各自的浏览器存储。

请使用设置中的导出功能备份业务数据。源码文件或 GitHub 提交历史不能代替个人数据备份；不要把包含个人记录的 JSON 备份提交到公开仓库。

**当前目录版本的导入操作会覆盖全部本地数据。** 导入前先导出当前数据。本说明按当前 `index.html` 核对：其中未包含此前另一份版本的智能合并实现，不能将普通导入当作双端合并使用。

## 在电脑上检查手机显示

1. 在 Edge / Chrome 打开本地 `index.html`，按 F12，再按 Ctrl + Shift + M。
2. 使用响应式设备模式，分别检查 320、390、430 像素宽度。
3. 用长标题、长分类和多条日志检查封面墙、列表和详情弹窗。
4. 调大工作台字号，确认三个封面仍在同一行，文字、评分和操作按钮完整可见。
5. 检查分页、弹窗底部按钮，以及是否存在异常横向滚动。

模拟器能发现多数布局问题，正式发布前仍应在实体手机检查一次。测试数据应与日常业务数据分开。

## 修改历史

按北京时间（Asia/Shanghai）的自然日划分版本，使用日期版本号：同一天的所有修改归入同一个版本。以下记录按当前项目的实际修改整理，不代表 Git 提交时间；较早版本没有完整日志，不补写未经核实的历史。

### v2026.09.19 · 2026-09-19

**车辆管理**

- 修复电池健康度“周期记录”弹窗中“忽略本周期”和“重新使用”按钮未绑定处理逻辑的问题。
- 弹窗每次刷新重新获取周期数据，不再沿用打开时的快照；操作后立即更新按钮、已忽略标记及健康度汇总。
- 复用原有 ignoreSoh 字段及保存逻辑，不删除充电或行程记录，不改变健康度计算公式。
- 验证：按钮处理调用、忽略及恢复后的统计、序列化保存再加载、行程数据不变和脚本语法检查通过。上述为逻辑回归测试，尚未进行实体手机验证。

### v2026.09.17 · 2026-09-17

**影剧书与封面墙**

- 封面墙每行固定三个封面，标题最多一行，超长部分以省略号显示。
- 卡片下方显示数字评分和状态；已看完且有结束日期时显示“2026.9.17看完”，未填日期仍显示“看完”。评分与看完日期在同一行显示。完整标题、分类及起止日期保留在详情中。
- 封面详情补充“看完”和“弃了”，沿用已有状态、评分和感想保存功能。
- 电影和电视剧改用“记观看”：电影记录日期、时长；电视剧增加集数，支持季数和集数范围。书籍继续记录阅读页码和时长。
- 沿用原 readLogs 日志数组，电视剧新增 episode 字段；旧日志与累计时长保留，缺少集数时显示“未记录集数”。新增时长须为有效正数。

**手机显示**

- 调整封面摘要，减少卡片垂直长度；当天关于列数、标题行数的调整以本版本上述最终效果为准。
- 修复任务、工作留痕、身体记录等共用标题和标签的强制截断，允许分页栏及操作按钮换行。

**时光档案**

- 同一天、明确关联的任务和工作留痕合并展示，保留状态和工作时间，原始记录不删除。
- 不展示未完成任务、其关联留痕、未来日期留痕和未完成学习计划。
- 体重、腰围、臂围、臀围、胸围统一使用中文名称及单位。

**文档与验证**

- 新增 README，整理使用、备份、手机验证方法；更新日志采用一天一个版本。
- 已检查档案合并与筛选、观看表单保存、状态按钮调用、旧日志兼容及脚本语法。
- 手机适配初版进行了窄屏长文本和字体放大验证；最终三列、单行标题版本已核对样式，尚未完成实体手机复测。
- 展示调整不修改原始业务数据；观看日志只增加电视剧集数信息，兼容旧记录。

## 后续维护记录方式

- **同一天只维护一个版本**：在当天版本内补充或修订内容，不为每次修改单独创建版本标题。
- **跨天且有修改时新增版本**：例如 v2026.09.18；没有修改的日期不创建空版本。
- **新版本放在最上方**，已经结束的日期保留为历史版本。
- **当天反复调整的功能记录最终效果**，避免同一版本同时出现互相矛盾的说明；必要的兼容性变化和验证限制仍需保留。
- 修改代码时同步维护本文件，并与 index.html 一起提交。一天内可以多次 Git 提交，但仍属于同一个日期版本。

GitHub 的文件 History 用于查看每次提交的代码差异；本文件用于按日查看功能更新。页面运行时不会自动更新 README。
